services:
  db:
    image: postgres:16-alpine
    container_name: rss-sec-dashboard-postgres
    environment:
      POSTGRES_USER: rss
      POSTGRES_PASSWORD: rss
      POSTGRES_DB: rss
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rss -d rss"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: rss-sec-dashboard-app
    environment:
      NODE_ENV: production
      PORT: 4000
      DATABASE_URL: postgresql://rss:rss@db:5432/rss
      LOG_LEVEL: info
    ports:
      - "4000:4000"
    depends_on:
      db:
        condition: service_healthy
    command: sh -c "npx prisma migrate deploy && node dist/index.js"

volumes:
  postgres_data:
